// The fluidfunctionalism icon registry imports individual icons from icon
// packs that ship without type declarations. Declare the subpaths so the
// production build (next build runs tsc) type-checks.
declare module "@hugeicons/core-free-icons/*";
